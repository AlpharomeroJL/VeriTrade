# Build-in-public — finalist drafts

Tone: **calm, precise, production-minded.** No hype adjectives.

---

## Post A — Finalist submission (short)

**VeriTrade** — finalist build: a governed trading agent console where **risk routes first**, **intents are binding** (SHA-256 commitment), and every stage writes **validation artifacts**. Demo runs **paper-only**; **Kraken** shows up as a real **CLI order-draft surface** from the same intent. Built for judges who care about **controls + venue alignment**, not toy charts. `[link]` #TradingAgents

---

## Post B — Thread (4 posts)

1/ Finalist mode = **clarity**. We stopped adding features and shipped **evidence**: artifact chain, intent commitment, risk router verdicts — all visible in a **single operator UI**.  
2/ **Kraken** isn’t a gimmick here: it’s an **explicit execution surface** with typed drafts. **Paper** runs the demo safely; live stays **gated**.  
3/ **Trustless-agent framing** without chain theater: stable **agent id**, trust **signal contract** in API, optional **URI stub** for where registry metadata would live.  
4/ If you’re judging combined tracks, this is one codebase that reads **intentional** — not a generic trading toy. Screenshots + paste-ready copy in `docs/submission/`. `[link]`

---

## Post C — “What we refused to do”

Refused: live trading tonight, on-chain scope creep, auth/billing/multi-tenant.  
Shipped: **finalist polish**, **exact screenshot set**, **2–3 minute demo path**, **submission description** ready to paste.

---

## Post D — Video description

**VeriTrade (Finalist)** — 2.5 min: challenge alignment panel (identity + trust signals + Kraken draft), one governed cycle (pipeline + signed intent hash + artifact chain), operator pause/step. Paper execution. Open source: `[link]`. Chapters in `docs/submission/demo-flow-finalist.md`.
